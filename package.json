{
  "name": "convivencia-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build && npm --prefix backend run prisma:generate",
    "start": "node backend/src/index.js"
  }
}
